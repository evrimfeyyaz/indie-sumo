<% cache external_resource do %>
  <%= render 'shared/large_card',
             url:         external_resource.url,
             icon:        external_resource.icon.attached? ? external_resource.icon : nil,
             title:       external_resource.title,
             description: external_resource.description %>
<% end %>