<div class="resource-recommendation">
  <%= render 'shared/page_title', title: "Recommend a #{@category.title.singularize}" %>
  <%= render 'shared/page_description', description: "Do you think we are missing something in \"#{@category.title}\"? Let us know, and we will add it." %>

  <%= render 'form' %>
</div>